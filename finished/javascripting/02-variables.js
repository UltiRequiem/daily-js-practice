let anExample;
//  The above variable is declared, but it isn't defined (it doesn't yet reference a specific value)

//  Here's an example of defining a variable, making it reference a specific value:
const exampleTwo = 'a string';

const example = 'some string';

console.log(example);
