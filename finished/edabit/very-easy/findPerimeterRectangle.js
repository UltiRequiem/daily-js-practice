// function findPerimeter(length, width) {
//  return length * width;
// }

const findPerimeter = (length, width) => length * width;

// Find the perimeter of a rectangle
console.log(findPerimeter(20, 90));
