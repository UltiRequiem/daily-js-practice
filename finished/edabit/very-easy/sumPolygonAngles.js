/**
 * Return the total sum of internal angles (in degrees).
 * @param {number} n A number.
 * @returns {number} Total sum of internal angles.
 */

// function sumPolygon(n) {
//  return (n - 2) * 180;
// }

const sumPolygon = (n) => (n - 2) * 180;

console.log(sumPolygon(6));
